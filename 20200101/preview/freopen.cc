#include <iostream>
using namespace std;
int main()
{
    int x,y;
    cin >> x >> y; //键入的第一个数和第二个数之间，无论隔着几个空格都没关系
    cout<<x<<"  "<<y<<endl;
    /*freopen("test.txt", "w", stdout);  //将标准输出重定向到 test.txt文件
    if( y == 0 )  //除数为0则输出错误信息
        cerr << "error." << endl;
    else
        cout << x /y ;
    */
        return 0;
}
